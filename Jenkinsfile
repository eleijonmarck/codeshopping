#!/usr/bin/env groovy

stageUrl = 'localhost:8080'
stage 'CI'
node {
        stage('Build') {
                echo 'Building......'
        }
        stage('Test') {
                echo 'Testing..'
        }
        stage('Deploy') {
                echo 'Deploying....'
        }
}
